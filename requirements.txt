# Backend Dependencies
Flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
huggingface_hub>=0.21.0
transformers>=4.36.0

# Frontend Dependencies (install with npm or yarn, not pip)
# These are for reference only:
#   react
#   react-dom
#   react-scripts
#   tailwindcss
#   autoprefixer
#   postcss
#   @headlessui/react (if used for modals, etc.)
#   @heroicons/react (if used for icons)
# Install using: npm install <package> or yarn add <package>
