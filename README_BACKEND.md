# Backend for Online Learning Platform

## Run locally

```bash
pip install -r requirements.txt
python app.py
```

## Endpoints

### Status
- GET `/api/status` → `{ "status": "ok" }`

### AI
- POST `/api/ai` → `{ "reply": "..." }`

### Future
- AI service integration
- Courses API
